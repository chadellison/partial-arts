<div class="jumbotron">
  <%= link_to_home %>
  <h1>Belts</h1>
</div>

<% @belts.each do |belt| %>
  <h2><%= belt.color.capitalize %></h2>
  <div class="belt-<%= belt.color %> clearfix">
    <% belt.artists.each do |artist| %>
      <%= artist_div(artist) %>
    <% end %>
  </div>
<% end %>

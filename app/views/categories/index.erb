<div class="jumbotron">
  <h1>Categories</h1>
</div>

<% @categories.each do |category| %>
  <h2><%= category.name %></h2>
  <div class="clearfix">
    <% category.artists.each do |artist| %>
      <div class="artist">
        <img src="<%= artist.image_url %>" alt=""> <br>
        <h3><%= link_to_artist_show(artist) %></a></h3>
      </div>
    <% end %>
  </div>
<% end %>

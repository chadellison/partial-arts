<div class="jumbotron">
  <%= link_to_home %>

  <h1>Categories</h1>

</div>

<% @categories.each do |category| %>
  <h2><%= category.name %></h2>
  <div class="clearfix">
    <% category.artists.each do |artist| %>
      <%= artist_div(artist) %>
    <% end %>
  </div>
<% end %>
